/**
 * Aqui usa-se um método que importa e exporta ao mesmo tempo um objeto js
 * A ideia é poder usar o destruct dentro de outros arquivos.
 * Para usar o destruct, obrigatoriamente tem que criar o arquivo index.js (este arquivo),
 * e no conteúdo dele, haver os imports e exports respectivos, como abaixo:
 */

export { default as caixaAlta } from './utilidades';